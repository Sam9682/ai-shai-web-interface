Gemini prompt — Compute & Block, Milestone 3 (Deploy all components — SNC parity). Paste the text below verbatim into Gemini (Imagen). No reference image needed.

Generate a single horizontal 16:9 flat 2-D cartoon explainer illustration in the friendly style of a corporate onboarding deck. Use a soft pastel palette, clean rounded shapes with thin light line-art outlines, and simple smiling cartoon characters with minimal facial detail — just dot eyes and a small smile. Flat vector look on a light grey background panel: no 3-D, no isometric perspective, no heavy shading, no photorealism, no real human faces, no third-party brand logos (a clean "OVHcloud" word-mark rendered as plain type in a UI header is fine — it is our own product). Keep type readable; this panel is dense.

Across the very top runs a full-width bright yellow banner (hex #FFD400) with dark text reading "MILESTONE 3: DEPLOY ALL COMPONENTS", and a smaller dark subtitle "SNC parity · proxies · polished Landing Zone Manager · backups · observability". A small "M3" mark sits at the far right.

Below the banner the picture is divided into three equal-height horizontal layer bands stacked top to bottom, each with a small coloured label tab in its upper-left corner. Top band "LANDING ZONE" cream tint (#fffaeb), yellow border and tab. Middle band "CLOUD STORE" light blue tint (#f0f4fc), blue border and tab. Bottom band "OPCP CORE" light grey tint (#f1f3f8), dark grey border and tab.

In the TOP band (LANDING ZONE), on the far left draw a friendly L3 End User cartoon in a purple hoodie at a laptop with a speech bubble "My OVHcloud-style manager has everything" and label "L3 End User". In the centre draw a wide polished "Landing Zone Manager" product UI window: a blue top header bar with a clean "OVHcloud" word-mark, a breadcrumb "CloudStore · Acme · Compute", a bell icon and a user-avatar pill "M"; a light-blue left sidebar with nav items "Dashboard, Compute (selected, highlighted), Storage, Network, Backups, Monitoring, Identity, Horizon (highlighted as a power-user link)"; a top row of five stat cards — "COMPUTE: 5 VMs running", "STORAGE: 12 volumes · 1.2 TB", "BACKUPS: all OK · last 03:00 UTC", "ACCOUNTS: 3 active", "PLATFORM HEALTH: all proxies + all clusters green · SNC compliance audit live"; and a bottom row with a "Recent activity" panel (lines "14:30 my-vm started", "11:15 vol-02 created (50 GB)", "03:00 backup completed → S3") and a "CPU usage (24h)" panel with a small sparkline reading "avg 38% · peak 71%". Place a dark circular step badge with a white digit 4 on the window's upper-left.

In the MIDDLE band (CLOUD STORE), on the far left draw a friendly L2 IT-Admin cartoon in a light-blue shirt with a speech bubble "Daily backup to S3 for all accounts" and label "L2 IT-Admin". The centre showpiece of THIS band is a wide white card with a blue header reading "6 SNC Proxy Groups · in cbs-cp (the Service control plane)" and a right-aligned "SNC parity reached"; inside, a small matrix with column headers "GLOBAL · REGIONAL · DATA-PLANE", an "L2" row of three green chips "Global L2", "Regional L2", "Data-Plane L2", and an "L3" row of three green chips "Global L3", "Regional L3", "Data-Plane L3", with a yellow side-pill "proxy-redis (cache)" and a footer "TLS · Keystone token-audience attribution · tenant isolation · audit → LDP / Wazuh"; place a dark step badge with a white digit 3 on this card. The proxies belong on the L2 / CloudStore layer (they run inside cbs-cp), so this card sits in the middle band, NOT in the OPCP Core band below. Above or beside the proxy card draw a compact "CloudStore · Admin" UI tile (tabs Backup / Cross-account) showing a "Backup Destination" field "s3://acme-backups/opcp/ · daily 03:00" with a green "Scope: all accounts" row; place a dark step badge with a white digit 1 on the Backup Destination field. On the far right float a light-yellow sticky-note callout titled "SNC parity reached · end-to-end stack live" with bullets "6 SNC proxy groups (in cbs-cp)", "polished LZ Manager", "backups → S3", "cross-account admin", "observability + audit".

In the BOTTOM band (OPCP CORE) — infrastructure only, no proxies here. On the left draw a green "Cinder Backup" block (sub-rows "snapshots → tarball", "VM image exports") with a green arrow flowing right into a small "External S3 (customer-configurable)" cloud; place a dark step badge with a white digit 2 on the Cinder Backup block. Below it draw an "Observability stack" block with chips "Prometheus", "Grafana", "Loki", a yellow "LDP audit" pill and a yellow "Wazuh" pill, footed "scrapes every cluster · feeds SNC compliance audit". In the centre/right draw a compressed "from M1 + M2 (still healthy)" container of small cards: "OPCP Infra Operator (Compute + Storage + Keystone Controllers)", "Ceph", "Glance · PCI synced", "Nova hosts (tenant VMs across accounts)", and "cbs-cp compute substrate (hypervisor VMs hosting the Service control plane + the 6 proxies)".

Draw two green cross-layer arrows: one from the Backup Destination field (middle band) down to the Cinder Backup block (bottom band) labelled "backup destination set"; and one from the 6-proxy-groups card in the middle band up to the Landing Zone Manager window in the top band, labelled "proxies surface APIs + UI" (both endpoints are in the upper two bands — the proxies are NOT in the bottom band).

Along the bottom-right draw a dark rounded legend bar (hex #1c2230, white text) reading "SEQUENCE  1 L2 sets up backup → 2 Cinder pushes to S3 → 3 all 6 proxies live → 4 L3 uses the polished LZM". Step badges are filled dark circles (#1c2230) with a thin white outline and bold white digits. Ready components use a green accent (#34a853) on light green (#dff5e0).

Avoid any 3-D or isometric rendering, photorealism, real human faces, cluttered backgrounds, third-party tech logos, and any status or novelty markers such as "NEW", "BEFORE", "AFTER", "IN PROGRESS" or risk flags. Show only the end-state of Milestone 3. Do not draw any auto-recovery or HA badge — that is M4.
