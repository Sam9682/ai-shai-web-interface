Gemini prompt — Compute & Block, Milestone 2 (Start a VM — API-only Beta). Paste the text below verbatim into Gemini (Imagen). No reference image needed.

Generate a single horizontal 16:9 flat 2-D cartoon explainer illustration in the friendly style of a corporate onboarding deck. Use a soft pastel palette, clean rounded shapes with thin light line-art outlines, and simple smiling cartoon characters with minimal facial detail — just dot eyes and a small smile. Flat vector look on a light grey background panel: no 3-D, no isometric perspective, no heavy shading, no photorealism, no real human faces, no third-party brand logos. Keep generous negative space and make every text label crisp and legible.

Across the very top runs a full-width bright yellow banner (hex #FFD400) with dark text reading "MILESTONE 2: START A VM — API-only Beta", and a smaller dark subtitle "L3 End User · enabled manually by L2 IT-Admin · OpenStack API direct". A small "M2" mark sits at the far right of the banner.

Below the banner the picture is divided into three equal-height horizontal layer bands stacked top to bottom, each with a small coloured label tab in its upper-left corner. The top band is "LANDING ZONE" with a cream tint (#fffaeb), yellow border and tab. The middle band is "CLOUD STORE" with a light blue tint (#f0f4fc), blue border and tab. The bottom band is "OPCP CORE" with a light grey tint (#f1f3f8), dark grey border and tab.

In the TOP band (LANDING ZONE), on the far left draw a friendly L3 End User cartoon in a purple hoodie at a laptop, with a speech bubble "Deploy my-vm with a 100 GB volume — via the openstack CLI" and a label "L3 End User". In the centre draw a dark terminal window titled "Terminal — openstack CLI" showing this transcript: "$ openstack server create my-vm --flavor b3-8 --image debian-12 --volume vol-01" then a small result table with rows "name: my-vm", "status: ACTIVE", "flavor: b3-8 (8 GB · 2 vCPU)", "volume: vol-01 (100 GB)", then "$ openstack console url show my-vm → https://vnc.acme.opcp/...". Put a small caption under the terminal "OpenStack-compliant API · direct to the OPCP Infra API (no proxies in M2)" and place a dark circular step badge with a white digit 3 on the terminal's upper-left. On the far right float a light-yellow sticky-note callout (slightly rotated, soft drop shadow) titled "End users in enabled accounts run VMs · API-only Beta" with bullets "via OpenStack API (CLI / SDK)", "volumes attach", "VNC console (CLI)", "direct endpoint access", and a small grey footer "Proxies · Horizon · first-party VM App UI · BL auto-activation → all M3".

In the MIDDLE band (CLOUD STORE), on the far left draw a friendly L2 IT-Admin cartoon in a light-blue shirt at a laptop, with a speech bubble "Enable Compute & Block for this account — run the manual runbook" and a label "L2 IT-Admin". In the centre draw a small admin UI tile with a per-account toggle reading "Compute & Block: ON" for account "acme", and beside it a small dark command chip "kubectl apply -f keystone-domain-acme.yaml"; place a dark step badge with a white digit 1 here, and a caption "manual runbook + kubectl apply (no BL automation in M2)". On the right draw a chip representing the already-running "cbs-cp (Service control plane)" footed "exists since M1 · proxies + first-party UI come in M3"; place a dark step badge with a white digit 2 on the manual KeystoneDomain step that wires the account.

In the BOTTOM band (OPCP CORE) draw a green "OPCP Infra Operator" block containing a highlighted "Keystone Controller" chip with sub-rows "Keystone Domain · IdP federation · group/role mapping · project + users + app-creds"; the controller is the reconciler that fires the 6-resource KeystoneDomain chain. To its right draw a small "Glance images (basic pre-loaded subset)" chip and a "Compute · Nova hosts" container showing one node card "cpt-01" with a friendly blue VM box labelled "my-vm" (dot-eyed smile) and an attached cylinder "vol-01 (100 GB)" via "Cinder · Ceph RBD". Draw an arrow from the terminal in the top band running straight down to the OpenStack endpoints on these Nova hosts, carrying a pill label "OpenStack API — direct (no proxies in M2)"; place a dark step badge with a white digit 4 where the VM becomes active.

Draw one green cross-layer arrow from the manual KeystoneDomain step in the middle band down into the Keystone Controller in the bottom band, with a green pill label "KeystoneDomain applied".

Along the bottom-right draw a dark rounded legend bar (hex #1c2230, white text) reading "SEQUENCE  1 L2 enables manually → 2 Keystone Controller configures the account → 3 L3 creates a VM via the API → 4 VM runs with volume attached". Step badges are filled dark circles (#1c2230) with a thin white outline and bold white digits. Ready components use a green accent (#34a853) on light green (#dff5e0).

Avoid any 3-D or isometric rendering, photorealism, real human faces, cluttered backgrounds, third-party tech logos, and any status or novelty markers such as "NEW", "BEFORE", "AFTER", "IN PROGRESS" or risk flags. Show only the end-state of Milestone 2. Do not draw any proxy layer, Horizon UI, first-party CloudStore VM App UI, snapshots, or auto-recovery / HA — those are M3 or M4. The OpenStack API path in M2 is direct, with no proxies in front.
