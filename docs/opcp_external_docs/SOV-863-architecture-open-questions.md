# Jira `SOV-863` — `[OS] Architecture Decisions + Open Questions`

> Parent: `LVL2-18375`. Tracking Epic for active OSQs that are blocking or shaping the OS design. Each task below corresponds to one open question. Close tasks when the decision lands in [`decisions.md`](../decisions.md) (to create once the project moves past discovery).

---

## Tasks under this Epic

### `SOV-864` — OSQ-15: Provisioning engine — Rook vs pure TF

**Decision needed:** which engine drives day-2 Ceph lifecycle?

| Option | Summary |
|---|---|
| A — Pure TF + thin worker | TF drives Ironic/Neutron; custom reconciler for day-2. TF's weak spot for drift/heal. |
| B — Reuse OPCP Infra Operator | Deploy Storage Controller inside the Package. Couples to operator releases. |
| C — Rook + VCF | Package calls OpenStack/Ironic directly for BM; Rook manages Ceph day-2 inside `oss-cp`. **Emerging direction.** Solves OSQ-17 naturally via k8s Secrets. |

**Status:** Marc D confirmed VCF way 2026-05-27. Stephan Hohn left this workstream (2026-05-29). **Boris Behrens now owns Rook discovery.** Zafar Akhtar to support with Kubernetes. Starting with Hello World connectivity test. 3 `ceph-block-*` VMs in `paas-storage-block` available for the POC.

**Blocks:** `SOV-855` (Provisioning Engine)

---

### `SOV-865` — OSQ-17: RGW secrets — Keystone Controller → RGW config

**Decision needed:** how do RGW admin credentials (generated by the Keystone Controller at service-enable time) get injected into the RGW configuration?

Options:
* k8s Secrets in `oss-cp` — natural if Rook (OSQ-15C)
* External secrets manager
* Package-level secret refs in `cloudstore.yaml`

**Status:** Raised by Ibrahim 2026-05-27 as the remaining open part of OSQ-02. Resolution follows naturally from OSQ-15 if Rook is chosen.

**Blocks:** `SOV-856` (Dedicated Keystone / RGW wiring)

---

### `SOV-866` — OSQ-02: Keystone topology — single vs dual for SNC

**Decision needed:** does the OS Package deploy one Keystone per install or two (one for S3 users, one for S3 admins — the SNC 2-Keystone model)?

**Known:** Ibrahim confirmed 2026-05-27 that the SOV-254 Keystone Controller is compatible for both CB and OS. The 6 RGW config params are known. RGW admin user is created at service-enable time.

**Remaining:** agree on the SNC 2-Keystone topology with Florent / Vincent / Ibrahim in the dedicated OSQ-02 meeting.

**Blocks:** `SOV-856` (Dedicated Keystone) and `SOV-851` (MS3 SNC Ready)

---

### `SOV-867` — OSQ-03: BL capacity — when does the squad free up for OS?

**Track:** Business Logic squad (Vincent / Ibrahim) is fully booked on SNC until June. Object Storage needs BL for account activation, quota management, and usage/consumption.

**Questions:** when does BL capacity free up? What is the earliest OS can start BL integration? Is the July target still achievable?

**Owner:** Marc to confirm timeline with Vincent.

**Blocks:** `SOV-858` (BL Integration) and overall July schedule confidence

---

## Links

* Open questions (full table): [`open-questions.md`](../open-questions.md)
* Action plan: [`action-plan.md`](../action-plan.md)
