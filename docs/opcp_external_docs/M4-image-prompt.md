Gemini prompt — Compute & Block, Milestone 4 (Make it Airgapped). Paste the text below verbatim into Gemini (Imagen). No reference image needed.

Generate a single horizontal 16:9 flat 2-D cartoon explainer illustration in the friendly style of a corporate onboarding deck. Use a soft pastel palette, clean rounded shapes with thin light line-art outlines, and simple smiling cartoon characters with minimal facial detail — just dot eyes and a small smile. Flat vector look on a light grey background panel: no 3-D, no isometric perspective, no heavy shading, no photorealism, no real human faces, no third-party brand logos. This is the densest panel — keep type sizes readable.

Across the very top runs a full-width bright yellow banner (hex #FFD400) with dark text reading "MILESTONE 4: MAKE IT AIRGAPPED", and a smaller dark subtitle "full lifecycle control · auto-recovery · tiered storage · IT-Admin owns the cluster". A small "M4" mark sits at the far right.

Below the banner the picture is divided into three equal-height horizontal layer bands stacked top to bottom, each with a small coloured label tab in its upper-left corner. Top band "LANDING ZONE" cream tint (#fffaeb), yellow border and tab. Middle band "CLOUD STORE" light blue tint (#f0f4fc), blue border and tab. Bottom band "OPCP CORE" light grey tint (#f1f3f8), dark grey border and tab.

In the TOP band (LANDING ZONE), on the far left draw a friendly L3 End User cartoon in a purple hoodie at a laptop with a speech bubble "Insane tier · auto-recovery on. Done." and label "L3 End User". In the centre draw a "CloudStore VM App · Create Instance" form: row 1 a "NAME" field ("my-vm") and a "FLAVOR" field ("b3-8 (8 GB · 2 vCPU)"); row 2 a "STORAGE TIER" label with three pills side by side — "Standard (1 000 IOPS · 100 MB/s)", "High (3 000 IOPS · 200 MB/s)", and a highlighted-green selected "Insane (7 500 IOPS · 300 MB/s)" — with a dark CLI summary to the right "vol-01 · 100 GB · acme-net"; row 3 a yellow "Auto-recovery" row with a small yellow "HA" shield, the checked label "Auto-recovery", a sub-line "on host failure · RTO < 60 s", and a wide blue "CREATE" button. Place a dark circular step badge with a white digit 4 on the Storage Tier row. To the right draw a small running-VM preview card "my-vm · Active" (a blue VM box with a dot-eyed smile) with two pill badges "Auto-rec" (yellow) and "Insane" (green), footed "100 GB · vol-01 · cpt-04". On the far right float a light-yellow sticky-note callout titled "Airgapped cluster · full lifecycle" with bullets "add / drain / remove", "smooth upgrade", "auto-recovery on", "3 tiers (Std / High / Insane)", "live-migration".

In the MIDDLE band (CLOUD STORE), on the far left draw a friendly L2 IT-Admin cartoon in a light-blue shirt with a speech bubble "Drain cpt-02 for a firmware upgrade" and label "L2 IT-Admin". In the centre draw a wide "CloudStore · Admin · Cluster Lifecycle" window: a top action-button row "Add Node (blue filled), Smooth Upgrade, Downscale, Maintenance, Replace, Audit log (blue outline)"; a "Compute pool · Nova hosts" section with four green node cards "cpt-01 (healthy · 4 VMs · 42%)", "cpt-02 (healthy · 3 VMs · 67%, with the 'drain' action highlighted blue)", "cpt-03 (healthy · 5 VMs · 18%)", "cpt-04 (healthy · 2 VMs · 12%)" each with small per-node actions "drain · upgrade · remove"; and a "Storage pools · Ceph (3 tiers)" section with rows "Standard (4 NVMe · 1k IOPS)", "High (2 NVMe Pro · 3k IOPS)", a highlighted-green "Insane (1 NVMe Insane · 7.5k IOPS)", plus a yellow pill "Auto-recovery · default-on · RTO 60 s". Place a dark step badge with a white digit 2 on the top action-button row. On the far right draw a dark "AIRGAPPED MODE" panel (dark #1c2230 background, yellow accent text) headed "AIRGAPPED MODE · no external dependencies" with five green-check rows "local Harbor image registry", "local OS / firmware artifacts", "local audit + log retention", "local IdP fallback (break-glass)", "offline upgrades + cs-install".

In the BOTTOM band (OPCP CORE) draw the operator doing the lifecycle work autonomously: a green "OPCP Infra Operator" block with lifecycle CRD chips "MaintenanceWindow", "NodeDrain", "RollingUpgrade", "LiveMigrate", "HostFailureWatch"; place a dark step badge with a white digit 1 on it. To the right draw the Nova hosts with cpt-02 shown draining (its VMs migrating via arrows to cpt-01 and cpt-03) and a small "VM evacuated · RTO < 60 s" pill; place a dark step badge with a white digit 3 here. Show the three Ceph tier pools (Standard / High / Insane) as small cylinders, the Insane one highlighted green.

Draw a green cross-layer arrow from the Cluster Lifecycle action row (middle band) down to the OPCP Infra Operator lifecycle block (bottom band) labelled "drain / upgrade / live-migrate", and a thin arrow from the create form's tier selection down to the Insane Ceph pool.

Along the bottom-right draw a dark rounded legend bar (hex #1c2230, white text) reading "SEQUENCE  1 operator runs lifecycle CRDs → 2 L2 drains / upgrades a node → 3 VMs auto-recover / live-migrate → 4 L3 picks tier + auto-recovery". Step badges are filled dark circles (#1c2230) with a thin white outline and bold white digits. Ready components use a green accent (#34a853) on light green (#dff5e0).

Avoid any 3-D or isometric rendering, photorealism, real human faces, cluttered backgrounds, third-party tech logos, and any status or novelty markers such as "NEW", "BEFORE", "AFTER", "IN PROGRESS" or risk flags. Show only the end-state of Milestone 4. Everything must read as fully self-contained and offline — the AIRGAPPED MODE panel makes the "no external dependencies" point explicit.
