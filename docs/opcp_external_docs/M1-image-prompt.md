Gemini prompt — Compute & Block, Milestone 1 (Install the Service). Paste the text below verbatim into Gemini (Imagen). No reference image needed.

Generate a single horizontal 16:9 flat 2-D cartoon explainer illustration in the friendly style of a corporate onboarding deck. Use a soft pastel palette, clean rounded shapes with thin light line-art outlines, and simple smiling cartoon characters with minimal facial detail — just dot eyes and a small smile. Flat vector look on a light grey background panel: no 3-D, no isometric perspective, no heavy shading, no photorealism, no real human faces, no third-party brand logos. Keep generous negative space and make every text label crisp and legible.

Across the very top runs a full-width bright yellow banner (hex #FFD400) with dark text reading "MILESTONE 1: INSTALL THE SERVICE", and a smaller dark subtitle "Target: L2 IT-Admin". A small "M1" mark sits at the far right of the banner.

Below the banner the picture is divided into three equal-height horizontal layer bands stacked top to bottom, each with a small coloured label tab in its upper-left corner. The top band is "LANDING ZONE" with a cream tint (#fffaeb), yellow border and tab, subtitled "what L3 sees". The middle band is "CLOUD STORE" with a light blue tint (#f0f4fc), blue border and tab, subtitled "orchestration". The bottom band is "OPCP CORE" with a light grey tint (#f1f3f8), dark grey border and tab, subtitled "substrate".

In the TOP band (LANDING ZONE) nothing is active yet — the service is only per-account-enabled in M2 — so leave it mostly empty space. The only element is a light-yellow sticky-note callout floating in the upper-right (slightly rotated, soft drop shadow) titled "Compute & Block Service installed" with four bullets: "OpenStack populated", "Compute hosts in Nova", "Ceph cluster healthy", "Cinder backend wired (single default pool)". No characters here; the emptiness intentionally shows this tier exists but is dormant in M1.

In the MIDDLE band (CLOUD STORE), on the far left draw a friendly L2 IT-Admin cartoon in a light-blue shirt at a laptop, with a speech bubble "Pick the bare-metal nodes, click deploy" and a label "L2 IT-Admin". In the centre draw a CloudStore install UI window titled "Compute & Block — Install" with two checkbox columns side by side: a left column headed "Compute (3 nodes)" with three ticked rows "nuc-cpt-01", "nuc-cpt-02", "nuc-cpt-03", and a right column headed "Storage / Ceph (4 nodes)" with four ticked rows "nvme-st-01" through "nvme-st-04", a line beneath reading "Aggregate: default-pool", and a wide blue "LAUNCH DEPLOYMENT" button at the bottom; place a dark circular step badge with a white digit 1 on that button. On the right draw a purple "CloudStore TF" block with a terraform-style icon labelled "cs-cp tf-runner · renders Service plan", with footer lines "input: BM IDs (compute · storage)" and "calls the OPCP Infra API"; place a dark step badge with a white digit 2 on its upper-left. Thin blue arrows connect the L2 character to the install UI, and the install UI to the CloudStore TF block (labelled "triggers").

In the BOTTOM band (OPCP CORE) no persona is shown — once step 2 fires, the operator works autonomously. On the far left draw a blue "OPCP Infra API" block listing four small endpoint chips stacked vertically: "PUT /compute-pool", "PUT /compute-nodes", "PUT /storage-pool", "PUT /storage-nodes". In the centre-left draw a single green "OPCP Infra Operator" block containing two stacked controller chips — "Compute Controller (CRDs: ComputePool / Node · provisions BM · Nova · k3s join)" and "Storage Controller (CRDs: StoragePool / Node · provisions BM · Ceph · Cinder wire)" — to make clear both are reconcilers inside one operator binary; place a dark step badge with a white digit 3 on this block. On the right draw a grey "Bare-metal nodes (in OpenStack)" container with two rows: a "Compute (Nova)" row of three green node cards "cpt-01 / cpt-02 / cpt-03" each "k3s+Nova" plus a yellow pill "Nova hosts ready · 3 / 3 joined", and a "Storage (Ceph · Cinder backend)" row of four green node cards "st-01" through "st-04" each "k3s+Ceph" plus a yellow pill "Cinder backend · single default pool"; place a dark step badge with a white digit 4 on this container. Tucked beneath it draw a small separated sub-container labelled "Nova host aggregate · cloudstore-cp" holding two pastel-blue VM cards "cs-cp (local-storage VM)" and "cbs-cp (local-storage VM)" each footed "runs on an existing Compute hypervisor", with a small grey-italic caption "M1-simplified: no Ceph, no HA — substrate for M2". Thin green arrows connect the OPCP Infra API to the operator, and the operator to the bare-metal grid.

Draw one green cross-layer arrow dropping from the bottom of the CloudStore TF block in the middle band, crossing the band boundary, and landing specifically on the "OPCP Infra API" block (the leftmost block in the bottom band) — its arrowhead must touch the OPCP Infra API block and must NOT touch the OPCP Infra Operator. Important: the CloudStore TF only ever calls the OPCP Infra API; the operator is never called directly by the TF. The operator is reached only afterwards, by the separate thin green in-layer arrow that runs from the OPCP Infra API to the OPCP Infra Operator. Give the cross-layer arrow a green pill label mid-way reading "PUT /compute · /storage".

Along the bottom-right draw a dark rounded legend bar (hex #1c2230, white text) reading "SEQUENCE  1 L2 launches → 2 TF calls the OPCP Infra API → 3 the operator provisions → 4 Nova + Cinder ready". Step badges are filled dark circles (#1c2230) with a thin white outline and bold white digits. Layer band borders are thin and carry their accent colour (yellow / blue / grey). Ready components use a green accent (#34a853) on light green (#dff5e0).

Avoid any 3-D or isometric rendering, photorealism, real human faces, cluttered backgrounds, third-party tech logos (no AWS, no Azure, no generic Kubernetes wheels), and any status or novelty markers such as "NEW", "BEFORE", "AFTER", "IN PROGRESS" or risk flags. Show only the end-state of Milestone 1. Do not draw an L3 End User in the top band; do not draw an L1 Operator (their bare-metal prep happened before M1); do not draw any auto-recovery or HA badge (that is M4).
