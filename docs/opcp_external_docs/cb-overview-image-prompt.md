Gemini prompt — Compute & Block, project overview (the big picture). Paste the text below verbatim into Gemini (Imagen). No reference image needed. This is the single intro image for the project — same style as the milestone images, but high-level: only the important components, no step badges, no milestone-specific detail.

Generate a single horizontal 16:9 flat 2-D cartoon explainer illustration in the friendly style of a corporate onboarding deck. Use a soft pastel palette, clean rounded shapes with thin light line-art outlines, and simple smiling cartoon characters with minimal facial detail — just dot eyes and a small smile. Flat vector look on a light grey background panel: no 3-D, no isometric perspective, no heavy shading, no photorealism, no real human faces, no third-party brand logos. Keep it clean and uncluttered with generous negative space — this is a big-picture overview, not a detailed diagram, so show only the important components and keep labels short.

Across the very top runs a full-width bright yellow banner (hex #FFD400) with dark text reading "OPCP COMPUTE & BLOCK — the big picture", and a smaller dark subtitle "VM + Block storage as a CloudStore package on OPCP Core".

Below the banner the picture is divided into three equal-height horizontal layer bands stacked top to bottom, each with a small coloured label tab in its upper-left corner. The top band is "LANDING ZONE" with a cream tint (#fffaeb), yellow border and tab, subtitled "what the end user sees". The middle band is "CLOUD STORE — cbs-cp" with a light blue tint (#f0f4fc), blue border and tab, subtitled "the Service control plane (L2)". The bottom band is "OPCP CORE" with a light grey tint (#f1f3f8), dark grey border and tab, subtitled "the infrastructure substrate (L1)".

A unifying idea for the whole picture: each layer has a persona who manages or uses the platform through their own interface or panel — L3 through an end-user panel, L2 through an admin panel, L1 through an operator console — so the viewer sees that all three layers interact with the system via interfaces.

In the TOP band (LANDING ZONE), on the left draw a friendly L3 End User cartoon in a purple hoodie at a laptop with a small label "End user". In the centre draw a clean browser panel titled "Landing Zone — self-service" with a thin left sidebar showing a few nav items (Compute, Storage, Network, Keys), a main area with a small VM list of two rows ("my-vm · Active · b3-8" and "web-01 · Active") each with a tiny attached-volume tag ("vol-01 · 100 GB"), and a row of small action buttons at the bottom reading "Create instance", "Attach volume", "Console". This is the L3 end-user panel for self-service VMs + volumes. Keep this band light and end-user-only.

In the MIDDLE band (CLOUD STORE — cbs-cp), on the left draw a friendly L2 IT-Admin figure at a laptop using a clean "CloudStore Admin" panel — the L2 management interface. Give this panel some real detail: a small tab strip ("Install", "Accounts", "Backups", "Cross-account"), a short list with two rows ("acme · enabled" and "contoso · enabled"), and a row of management buttons reading "Install service", "Add account", "Configure backup", "Migrate VM", and "Audit log". In the centre draw one rounded container labelled "cbs-cp (Service control plane)" holding a single tidy row of four short component chips: "Service package (cloudstore.yaml + control/data TF)", "6 SNC proxy groups", "Landing Zone Manager UI", and "backups → S3". The proxies belong on this L2 layer (they run inside cbs-cp), not in the OPCP Core band below.

In the BOTTOM band (OPCP CORE), on the left draw a friendly L1 DC Operator figure at a small "OPCP Core operator console" panel (the L1 management interface — enroll hardware · OpenStack · operator status). Then draw a left-to-right flow of four clean blocks: an "OPCP Infra API" block, then an "OPCP Infra Operator" block with three small controller chips "Compute · Storage · Keystone" (to show the controllers are reconcilers inside one operator binary), then a "Nova hosts" container with a couple of node cards holding tenant VMs and Cinder/Ceph volumes, and a "Ceph cluster + Glance" block. Draw a single thin arrow from the cbs-cp container in the middle band down into this band, landing specifically on the "OPCP Infra API" block (not the operator), labelled "calls the OPCP Infra API"; and a thin in-band arrow from the OPCP Infra API to the OPCP Infra Operator to the Nova hosts.

Along the right edge, draw one thin vertical ribbon spanning the three bands with the small text "Keycloak: L1 OPCP Core ↔ L2 CloudStore → L3 per-Account realms" to show the 3-tier identity federation at a glance.

In a clear area (lower-right of the middle band) float a light-yellow sticky-note callout (slightly rotated, soft drop shadow) titled "Bring an SNC-class product into CloudStore" with three short bullets: "VM + Block as a package", "API-only Beta first, then SNC parity", "ultimately air-gapped".

Do not draw any milestone banner, numbered step badges, or sequence legend — this is the overall picture, not a single milestone. Avoid any 3-D or isometric rendering, photorealism, real human faces, cluttered backgrounds, third-party tech logos, and any status or novelty markers such as "NEW", "BEFORE", "AFTER", "IN PROGRESS" or risk flags. Keep it simple, friendly and high-level.
