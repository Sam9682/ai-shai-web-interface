# For production, use your actual domain (e.g., https://api.yourdomain.com/api)
# For local development with Docker, use http://ai-hypervisia:6000/api
# Using relative path /api works when frontend and backend are on the same domain
VITE_API_BASE_URL=/api
